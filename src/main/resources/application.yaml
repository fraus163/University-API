spring:
  application:
    name: university-api
    secret: ${JWT_SECRET:NudkeIDktep24kLKR4924Lfnke3hoPsnx}
    expirationMs: ${JWT_EXPIRATION_MS:2160000}
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:university_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_batch_fetch_size: 20